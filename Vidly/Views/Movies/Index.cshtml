@model IEnumerable<Vidly.Models.Movie>
@{
    ViewBag.Title = "Movies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

<ul>
    @{
        if (!Model.Any())
        {
            <p> Currently there are no movies.</p>
        }
        else
        {
            <table>
                <thead>
                    <tr>
                        <td>Name</td>
                        <td>Genre</td>
                    </tr>
                </thead>
                <tbody>
                    
                    @foreach (var movie in Model)
                    {
                         <tr><td>@Html.ActionLink(movie.Name, "GetDetails", "movies", new { id = movie.Id }, null)</td>
                                <td>@movie.GenreType.GenreName</td></tr>              
                    }
             
            </tbody>
            </table>

        }
    }
</ul>

